name: Example Workflow  

on:  
  push:  
    branches:  
      - main  

jobs:  
  check-sender:  
    runs-on: ubuntu-latest  
    steps:  
      - name: Check sender ID  
        run: |  
          echo "Sender ID: ${{ github.event.sender.id }}"
